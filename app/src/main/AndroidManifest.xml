<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.netshoesgistchallenge">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_netshoes1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_netshoes1_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeHome">
        <activity android:name=".ui.favorite.FavoriteListActivity"></activity>
        <activity android:name=".ui.details.DetailGistActivity" />
        <activity android:name=".ui.home.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>